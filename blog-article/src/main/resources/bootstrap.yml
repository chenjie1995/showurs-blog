server:
  port: 9003

spring:
  application:
    name: blog-article
  cloud:
    config:
      name: blog-cloud
      profile: dev
      discovery:
        enabled: true
        service-id: blog-config

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/
