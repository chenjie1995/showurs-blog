server:
  port: 9003

spring:
  application:
    name: blog-article
  cloud:
    config:
      discovery:
        enabled: true
        service-id: blog-config

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/