spring:
  cloud:
    config:
      profile: prod
      discovery:
        enabled: true
        service-id: showurs-blog-config
      label: master

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/